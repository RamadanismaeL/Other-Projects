import { Routes } from '@angular/router';
import { FormularioComponent } from './_pages/formulario/formulario.component';

export const routes: Routes = [
  { path: '', component: FormularioComponent }
];
